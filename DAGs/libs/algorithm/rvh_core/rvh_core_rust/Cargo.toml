# D:\city_chain_project\DAGs\libs\algorithm\rvh_core\rvh_core_rust\Cargo.toml
[package]
name    = "rvh_core_rust"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
# --- FFI & ランタイム連携 ---------------------------
pyo3                  = { version = "0.25", features = ["extension-module"] }
pyo3-async-runtimes   = { version = "0.25", default-features = false, features = ["tokio-runtime"] }
tokio                 = { version = "1.38", features = ["rt-multi-thread", "macros"] }

# --- コアロジック -----------------------------------
blake2     = "0.10"
hex        = "0.4"
anyhow     = "1"
serde      = { version = "1.0", features = ["derive"] }
thiserror  = "1.0"
clap       = { version = "4.5", features = ["derive"] }
indoc      = "1"

[dev-dependencies]
criterion    = { version = "0.5", features = ["html_reports"] }
assert_cmd   = "2"
predicates   = "3"
pyo3         = { version = "0.25", features = ["auto-initialize"] }
serde_json   = "1"
walkdir      = "2.5"

[lib]
name       = "rvh_core_rust"
crate-type = ["cdylib", "rlib"]
path       = "src/lib.rs"

[[bin]]
name = "main_rvh_core"
path = "src/main_rvh_core.rs"

[[bench]]
name    = "bench_hrw_score"
harness = false
